# Navigation Tests

- linking.test.ts — Module: linking config; Flows: prefixes include default schemes; adds web base URL when configured.
- navigationRef.test.ts — Module: navigationRef; Flow: exports a valid navigation ref instance.
- RootStack.test.tsx — Component: RootStack; Flow: registers expected stack screens and config (initial route, header hidden).
- RootTabs.test.tsx — Component: RootTabs; Flow: tab labels and icons are configured per route.
