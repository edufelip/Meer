# Thrift Screen Tests

- BrechoFormScreen.test.tsx — Screen: BrechoFormScreen; Flows: render create header; validate required fields; update existing store; block invalid Instagram; upload photos (gallery + camera); geocode suggestion + submit with coords; reorder photos; mark deleted photos; block when exceeding photo limit.
- ThriftDetailScreen.test.tsx — Screen: ThriftDetailScreen; Flow: renders store details.
