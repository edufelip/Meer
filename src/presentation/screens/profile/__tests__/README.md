# Profile Screen Tests

- ProfileScreen.test.tsx — Screen: ProfileScreen; Flows: renders cached profile; falls back to JWT payload when cache missing; navigates to myContents from "Criar Conteúdo".
- ProfileScreenWithUser.test.tsx — Screen: ProfileScreenWithUser; Flows: renders user name when found; empty state when user missing.
- EditProfileScreen.test.tsx — Screen: EditProfileScreen; Flows: renders header; disables save when name is missing.
