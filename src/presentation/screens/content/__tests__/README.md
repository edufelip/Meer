# Content Screen Tests

- ContentsScreen.test.tsx — Screen: ContentsScreen; Flow: renders header with initial route params.
- ContentDetailScreen.test.tsx — Screen: ContentDetailScreen; Flows: renders content details; back button navigates back.
- MyContentsScreen.test.tsx — Screen: MyContentsScreen; Flow: renders header after data load.
- EditContentScreen.test.tsx — Screen: EditContentScreen; Flows: renders header; validates missing fields; updates existing content; removes selected media; delete confirmation + confirm delete; dirty-state back prompt; create flow uploads media and saves content.
